# Core dependencies
opencv-python>=4.5.0
numpy>=1.21.0
matplotlib>=3.3.0
Pillow>=8.0.0
scipy>=1.7.0
scikit-image>=0.18.0
tqdm>=4.62.0
imutils>=0.5.4

# Fingerprint enhancement (you may need to install this separately)
# fingerprint-enhancer

# Note: External tools required (install separately):
# - NIST NBIS (for mindtct minutiae extraction)
# - NIST NFIQ (for fingerprint quality assessment)
# See external_tools/README.md for installation instructions